

export interface Note {
    notes: [{
        id?: string;
        title?: string;
        content?: string;
        createdAt?: string;
        updatedAt?: string;
        author?: string;
    }]
}